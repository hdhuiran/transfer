import { createApp } from "vue";
import "./style.css";
import App from "./App.vue";
// 接入自定义UI下的所有组件
// import HdhUI from "../modules/hdh-ui";
// import Transfer from "../modules/hdh-ui/Transfer";

// ues这里接入对于的接入名
// createApp(App).use(Transfer).mount("#app");

createApp(App).mount("#app");
